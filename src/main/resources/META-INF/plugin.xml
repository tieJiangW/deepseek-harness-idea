<idea-plugin>
    <id>com.deepseek.harness.idea</id>
    <name>DeepSeek Harness</name>
    <vendor url="https://github.com/tieJiangW/deepseek-harness-idea">DeepSeek Harness Dev</vendor>
    <icon>/icons/dsh-logo-512.png</icon>

    <description><![CDATA[
        <h3>English</h3>
        <p><b>DeepSeek Harness</b> embeds the full DeepSeek Harness AI agent workbench directly inside
        IntelliJ IDEA. Chat with the agent in an embedded web UI, let it read and modify your project
        files, and review its changes with native diff tooling — a Qoder-like AI coding experience.</p>

        <h4>Key features</h4>
        <ul>
            <li><b>Embedded Web UI</b> — the full DeepSeek Harness interface (chat, sessions, goals,
            workflows) runs in an IDE tool window via JCEF.</li>
            <li><b>Workspace-bound agent</b> — the agent works with your current project directory as
            its workspace (auto-registered on startup); it can read, create and modify project files.</li>
            <li><b>IDE context via MCP</b> — the agent can read your current editor selection, open
            files and project tree, and open/reveal files, through the <code>mcp__ide__*</code> tools.</li>
            <li><b>Send selection to DSH</b> — right-click any code selection to send a compact file
            reference (<code>@path#L1-14</code>) to the harness; the full selection stays available to
            the agent even if injection fails (clipboard fallback).</li>
            <li><b>Review &amp; restore</b> — a baseline snapshot lets you diff agent changes
            (modified/new/deleted), restore single or all files, ignore, or rebaseline.</li>
            <li><b>Self-contained runtime</b> — Node.js and the DeepSeek Harness runtime are bundled
            with the plugin and extracted on first use; no separate installation or network needed.</li>
            <li><b>Multi-project &amp; lifecycle</b> — one instance per project (max 3 concurrent),
            processes are terminated on project close / IDE exit; crash auto-restart with backoff.</li>
            <li><b>Logs &amp; diagnostics</b> — a DSH Log tab shows the dsh process output; crash
            notifications and a configurable log level help you debug.</li>
            <li><b>Robust runtime provisioning</b> — the per-platform runtime downloads on first use with a live
            progress bar and cancel, is SHA-256 verified, and can be imported from a local zip for offline use.</li>
            <li><b>Bilingual UI</b> — all plugin UI follows your IDE language (English / 简体中文).</li>
        </ul>

        <h4>Getting started</h4>
        <ol>
            <li>Open the <b>DeepSeek Harness</b> tool window (right edge) — the bundled runtime is
            extracted automatically on first open.</li>
            <li>Go to <b>Settings → Tools → DeepSeek Harness</b>, enter your <b>DeepSeek API Key</b>
            (or import it from <code>~/.dsh</code>), and apply.</li>
            <li>Start a conversation; ask the agent to read your code, or select code and use
            <i>Send Selection to DSH</i> to hand it context.</li>
            <li>Use <i>Review changes</i> in the tool window to inspect and restore agent edits.</li>
        </ol>

        <h4>Runtime &amp; offline</h4>
        <p>Node.js and the DeepSeek Harness runtime are resolved per platform (Windows / macOS ARM64 &amp; x64 /
        Linux x64) and downloaded once on first use (SHA-256 verified), then reused from
        <code>&lt;config&gt;/dsh-idea/runtime/&lt;version&gt;/</code>. To use a locally prepared runtime instead,
        set the <code>DSH_IDEA_RUNTIME</code> environment variable (or the <i>Runtime directory</i> field in
        Settings) to a directory containing <code>node/</code> and <code>dsh/</code>. The <i>Runtime download URL</i>
        setting overrides the official download source (e.g. an internal mirror) but does not change where the
        runtime is stored.</p>

        <h3>中文</h3>
        <p><b>DeepSeek Harness</b> 把 DeepSeek Harness 智能体工作台完整嵌入 IntelliJ IDEA：在 IDE
        内嵌的 Web 界面中与智能体对话，让它读写你的项目文件，并用原生 diff 工具审查它的改动——
        类 Qoder 的 AI 编程体验。</p>

        <h4>主要功能</h4>
        <ul>
            <li><b>内嵌 Web UI</b> — 完整 DeepSeek Harness 界面（对话、会话、目标、工作流）通过
            JCEF 在 IDE 工具窗口中运行。</li>
            <li><b>以项目为工作区</b> — 智能体以当前项目目录为工作区（启动时自动注册）；可读取、
            新建、修改项目文件。</li>
            <li><b>MCP 桥接 IDE 上下文</b> — 智能体可通过 <code>mcp__ide__*</code> 工具读取当前
            选中代码、打开的文件、项目树，并打开/定位文件。</li>
            <li><b>发送选中代码到 DSH</b> — 右键选中代码即可发送紧凑文件引用
            （<code>@路径#L1-14</code>）给智能体；即使注入失败，完整选中代码仍可被智能体获取
            （剪贴板兜底）。</li>
            <li><b>审查与还原</b> — 基线快照让您可以 diff 智能体的改动（修改/新增/删除），
            还原单个或全部文件、忽略、或重新基线。</li>
            <li><b>自包含运行时</b> — Node.js 与 DeepSeek Harness 运行时随插件打包，首次使用自动
            解压；无需单独安装或联网。</li>
            <li><b>多项目与生命周期</b> — 每项目一个实例（并发上限 3）；项目关闭 / IDE 退出时
            自动终止进程；崩溃自动退避重启。</li>
            <li><b>日志与诊断</b> — DSH Log 标签页显示 dsh 进程输出；崩溃通知与可配置日志级别
            便于排查。</li>
            <li><b>可靠的运行时下发</b> — 首次使用按平台下载运行时，带实时进度条与取消、SHA-256 校验，
            也可从本地压缩包离线导入。</li>
            <li><b>中英双语界面</b> — 插件 UI 跟随 IDE 语言（English / 简体中文）。</li>
        </ul>

        <h4>快速上手</h4>
        <ol>
            <li>打开 <b>DeepSeek Harness</b> 工具窗口（右侧）——首次打开自动解压内置运行时。</li>
            <li>进入 <b>设置 → Tools → DeepSeek Harness</b>，填写 <b>DeepSeek API Key</b>
            （或从 <code>~/.dsh</code> 一键导入）并应用。</li>
            <li>开始对话；让智能体阅读你的代码，或选中代码后使用 <i>发送选中代码到 DSH</i>
            把上下文交给它。</li>
            <li>在工具窗口使用 <i>审查改动</i> 查看并还原智能体的修改。</li>
        </ol>

        <h4>运行时与离线</h4>
        <p>Node.js 与 DeepSeek Harness 运行时按平台（Windows / macOS ARM64 &amp; x64 / Linux x64）解析，首次使用
        一次性下载（SHA-256 校验），之后复用 <code>&lt;config&gt;/dsh-idea/runtime/&lt;version&gt;/</code>。如需改用
        本地已备好的运行时，可设置 <code>DSH_IDEA_RUNTIME</code> 环境变量（或设置页「运行时目录」）指向一个包含
        <code>node/</code> 与 <code>dsh/</code> 的目录；「Runtime download URL」用于覆盖官方下载源（如内网镜像），
        但不会改变运行时的落盘目录。</p>
    ]]></description>

    <change-notes><![CDATA[
        <h3>English / 中文</h3>
        <h4>v0.2.1</h4>
        <ul>
            <li><b>Fixed a first-use runtime download failure.</b> On some setups the per-platform runtime could not be fetched: the temporary download file is written next to the runtime directory, whose parent directory does not exist yet on a fresh install, causing a <code>NoSuchFileException</code> ("download failed" with no progress). The plugin now creates the directory before writing, so first use downloads and verifies the runtime correctly.</li>
            <li><b>修复首次使用运行时下载失败的问题。</b>部分环境下按平台运行时无法下发：临时下载文件写在运行时目录旁，而全新安装时其父目录尚不存在，导致 <code>NoSuchFileException</code>（报"下载失败"且无进度）。插件现先创建目录再落盘，首次使用即可正确下载并校验运行时。</li>
        </ul>
        <ul>
            <li><b>More reliable runtime download.</b> The download client now uses a pooled, HTTP/2-capable client with a browser User-Agent, a relaxed 60s connect timeout and a configurable read timeout, plus backoff retries. First-use runtime downloads now succeed even on slow or unstable networks (e.g. GitHub access from mainland China), instead of giving up after a short timeout.</li>
            <li><b>更可靠的运行时下载。</b>下载改用带连接池、支持 HTTP/2 的客户端，并附带浏览器用户代理、放宽到 60 秒的连接超时与可配置的读取超时，以及退避重试。即便在慢速或不稳定网络（如国内访问 GitHub）下，首次运行时下载也能成功，而不再因短超时即放弃。</li>
        </ul>
        <ul>
            <li><b>Download progress bar with cancel.</b> The tool window now shows a live progress bar (bytes / MB / speed) while fetching the runtime, with progress states ("connecting / verifying / downloading") and a cancel action; it no longer blocks the UI or sits silently at 0%.</li>
            <li><b>运行时下载进度条（可取消）。</b>工具窗口在获取运行时期间会显示实时进度条（字节 / MB / 速率），并给出"正在连接/校验/下载"状态与取消入口；不再阻塞界面或停在 0% 无反应。</li>
        </ul>
        <ul>
            <li><b>Import a local runtime zip.</b> Settings and the error card now offer "Choose local runtime zip…", letting you point the plugin at an already-downloaded per-platform runtime zip (validated, SHA-256 checked against a sidecar) to start completely offline.</li>
            <li><b>导入本地运行时包。</b>设置页与错误卡新增"选择本地运行时压缩包…"，可指向已下载好的按平台运行时 zip（校验有效性并按侧车做 SHA-256 校验），完全离线即可启动。</li>
        </ul>
        <ul>
            <li><b>Clear diagnostics.</b> Settings shows the exact per-platform runtime download URL (down to the file) with one-click copy, and the runtime download timeout is configurable. On failure the error card shows the exact URL that failed and the underlying cause, plus "Restart harness" and a route to Settings.</li>
            <li><b>更清晰的诊断。</b>设置页回显当前平台完整的运行时下载地址（精确到文件）并可一键复制，下载超时可配置；失败时错误卡给出失败的具体地址与根因，并提供"重启 Harness"与直达设置入口。</li>
        </ul>
        <h4>v0.2.0</h4>
        <ul>
            <li>Fixed an install-time "invalid plugin descriptor" error on some IDE versions: the optional
            dependency on the JCEF module now declares its required <code>config-file</code>, so the plugin
            descriptor is valid on IntelliJ 2024.1–2026.2.</li>
            <li>修复部分 IDE 版本安装时报 "invalid plugin descriptor" 的问题：JCEF 模块的可选依赖现补上
            必填的 <code>config-file</code>，使插件描述符在 IntelliJ 2024.1–2026.2 上均合法。</li>
        </ul>
        <ul>
            <li>Cross-platform runtime: the plugin now runs on IntelliJ IDEA on macOS and Linux hosts, not only Windows. The Node.js + DeepSeek Harness runtime is resolved per platform and downloaded once on first use (SHA-256 verified); existing Windows installs reuse the already-extracted runtime seamlessly.</li>
            <li>跨平台运行时：插件现可在 macOS 与 Linux 主机上的 IntelliJ IDEA 运行，而不再仅支持 Windows。Node.js 与 DeepSeek Harness 运行时按平台解析，首次使用一次性下载（SHA-256 校验）；存量 Windows 安装会无缝复用已解压的运行时。</li>
        </ul>
        <ul>
            <li>Thin &amp; configurable package: the plugin zip no longer bundles the ~93MB runtime by default (Marketplace-friendly); the per-platform runtime is fetched on demand. An optional fat zip (<code>-Pthin=false</code>) remains available for offline / air-gapped installs, and the runtime download base URL is configurable in Settings.</li>
            <li>瘦身与可配置包：插件包默认不再内置约 93MB 运行时（利于 Marketplace）；按平台运行时按需获取。仍可构建含运行时的 fat 包（<code>-Pthin=false</code>）用于离线/内网安装；运行时下载地址可在设置中覆盖。</li>
        </ul>
        <h4>v0.1.3</h4>
        <ul>
            <li>Per-project DSH_HOME: workspace and sessions are now isolated per project, so switching
            projects no longer mixes up chat history and agent workspaces.</li>
            <li>每项目独立 DSH_HOME：工作区与会话按项目隔离，切换项目不再串台（聊天记录、智能体工作区各自独立）。</li>
        </ul>
        <ul>
            <li>"DSH 一键解释" in the Run console: select a log snippet, right-click and choose it to send
            the log to the agent for an instant explanation — great for "what went wrong" debugging.</li>
            <li>运行控制台新增「DSH 一键解释」：选中日志右键即可一键提交给智能体解释，排查报错更省心。</li>
        </ul>
        <ul>
            <li>Legacy session migration: after upgrading, previous chat sessions and projection caches are
            automatically migrated with correct titles — no more missing or mis-titled history.</li>
            <li>旧会话升级迁移：升级后自动迁移历史会话与投影缓存，标题正确，历史不再丢失。</li>
        </ul>
        <ul>
            <li>API key masking &amp; global sync: the API key is now shown masked in Settings and kept in
            sync across all projects via a single global credential file.</li>
            <li>API Key 脱敏与全局同步：设置页密钥脱敏回显，所有项目通过全局凭据文件保持同步。</li>
        </ul>
        <ul>
            <li>Fixed a credential conflict where injecting <code>DEEPSEEK_API_KEY</code> into the runtime
            environment could make the harness reject the key; the browser is no longer auto-opened on
            startup (<code>--no-open</code>).</li>
            <li>修复凭据冲突：不再向运行时注入 <code>DEEPSEEK_API_KEY</code> 环境变量（避免被 dsh 拒绝）；
            启动不再自动弹出浏览器（<code>--no-open</code>）。</li>
        </ul>
        <ul>
            <li>Bundled DeepSeek Harness runtime upgraded to 0.1.1-rc.2; repository source integrity fixed
            (runtime package sources are now fully tracked in git).</li>
            <li>内置 DeepSeek Harness 运行时升级至 0.1.1-rc.2；修复仓库源码完整性（runtime 包源码已全部入库）。</li>
        </ul>
        <h4>v0.1.2</h4>
        <ul>
            <li>JCEF compatibility on IntelliJ 2026.2: added an (optional) dependency on the bundled
            "Web Browser (JCEF)" plugin (<code>com.intellij.modules.jcef</code>). Since 2026.2 JCEF is no
            longer part of the platform core, so plugins using <code>JBCefBrowser</code> must declare this
            dependency or the classes are not visible at runtime (NoClassDefFoundError).</li>
            <li>2026.2 JCEF 兼容修复：新增对内置 "Web Browser (JCEF)" 插件
            （<code>com.intellij.modules.jcef</code>）的（可选）依赖。2026.2 起 JCEF 不再属于平台核心，
            使用 <code>JBCefBrowser</code> 的插件必须声明该依赖，否则运行时类不可见
            （NoClassDefFoundError）。</li>
        </ul>
        <ul>
            <li>When JCEF still fails to initialize, the tool window now shows the concrete exception plus
            check hints (enable the JCEF plugin / run the IDE on a JBR with JCEF / restart).</li>
            <li>JCEF 初始化失败时，工具窗口现在会显示具体异常与排查建议（启用 JCEF 插件 /
            以含 JCEF 的 JBR 运行时启动 / 重启）。</li>
        </ul>
        <h4>v0.1.1</h4>
        <ul>
            <li>Compatibility: now supports IntelliJ IDEA up to 2026.2 (build 262.*). Previously the
            plugin only declared support up to 2025.1 (build 251.*), so newer IDEs refused to load it.</li>
            <li>兼容性：现在支持至 IntelliJ IDEA 2026.2（build 262.*）。此前仅声明支持到 2025.1
            （build 251.*），新版 IDE 会拒绝加载。</li>
        </ul>
        <ul>
            <li>Removed dependency on the platform-bundled Gson JSON library (being phased out by
            JetBrains); JSON serialization is now self-contained and version-independent.</li>
            <li>移除对平台自带 Gson JSON 库的依赖（JetBrains 正逐步移除该库）；JSON 序列化改为
            自包含实现，与平台版本无关。</li>
        </ul>
        <h4>v0.1.0</h4>
        <ul>
            <li>Menu icon for "Send Selection to DSH" now matches the plugin icon.
            「发送选中代码到 DSH」右键菜单图标与插件图标一致。</li>
        </ul>
        <ul>
            <li>Distinct icons for the tool window actions: Settings (gear), Open in browser (web),
            Review changes (diff), Restart (restart).</li>
            <li>工具窗口动作区分图标：设置（齿轮）/ 浏览器打开（Web）/ 审查改动（Diff）/ 重启（Restart）。</li>
        </ul>
		<ul>
            <li>Plugin initialization completed, achieving basic interaction functions.</li>
            <li>插件初始化，完成基本交互功能。</li>
        </ul>
        
    ]]></change-notes>

    <depends>com.intellij.modules.platform</depends>

    <!-- 2026.2 起 JCEF 从平台核心拆分为内置插件 com.intellij.modules.jcef（"Web Browser (JCEF)"），
         使用 JCEF（JBCefBrowser）的插件须显式声明依赖，否则运行时 NoClassDefFoundError。
         该别名自 2025.3.1 (build 253) 引入；为兼容 2024.1–2025.3（JCEF 在平台核心、无此插件 id），
         用 optional 依赖：253+ 建立依赖使 JCEF 类可见，241–252 缺失时忽略（走核心 JCEF）。
         NOTE：optional=true 时 schema 要求必须给出 config-file（附加配置描述文件，见 META-INF/jcef.xml），
         否则加载期报 "invalid plugin descriptor"。见 docs/PROJECT_NOTES.md "2026.2 JCEF 拆分"。 -->
    <depends optional="true" config-file="jcef.xml">com.intellij.modules.jcef</depends>

    <idea-version since-build="241" until-build="262.*"/>

    <actions>
        <!-- Step 4：编辑器右键发送选中代码到 DSH（图标与插件工具窗口一致，便于识别归属） -->
        <action id="dsh.sendSelection"
                class="com.deepseek.harness.idea.ui.SendSelectionAction"
                text="Send Selection to DSH"
                description="Send the current editor selection to the embedded DeepSeek Harness"
                icon="/icons/dsh-toolwindow.svg">
            <add-to-group group-id="EditorPopupMenu" anchor="first"/>
        </action>

        <!-- 运行控制台右键"DSH 一键解释"：选中日志作为用户问题自动提交（不等待确认）。
             ConsoleView.PopupMenu 为 Run 控制台右键组（2024.1 ConsoleViewImpl.java:93 与
             2026.2 ConsoleViewImpl.kt:1668 同 id；见 docs/DESIGN.md §3.11）。 -->
        <action id="dsh.sendLogExplanation"
                class="com.deepseek.harness.idea.ui.SendLogExplanationAction"
                text="DSH 一键解释"
                description="Send the selected console log to the embedded DeepSeek Harness for explanation"
                icon="/icons/dsh-toolwindow.svg">
            <add-to-group group-id="ConsoleView.PopupMenu" anchor="first"/>
        </action>
    </actions>

    <applicationListeners>
        <!-- Step 5：项目关闭释放实例 + IDE 退出兜底终止 Node -->
        <listener class="com.deepseek.harness.idea.runtime.DshLifecycleManager"
                  topic="com.intellij.openapi.project.ProjectManagerListener"/>
        <listener class="com.deepseek.harness.idea.runtime.DshAppLifecycleListener"
                  topic="com.intellij.ide.AppLifecycleListener"/>
    </applicationListeners>

    <extensions defaultExtensionNs="com.intellij">
        <toolWindow id="DeepSeek Harness" anchor="right"
                    factoryClass="com.deepseek.harness.idea.ui.DshToolWindowFactory"
                    icon="/icons/dsh-toolwindow.svg"/>

        <applicationService serviceImplementation="com.deepseek.harness.idea.settings.DshSettingsState"/>
        <applicationService serviceImplementation="com.deepseek.harness.idea.runtime.DshHomeManager"/>
        <applicationService serviceImplementation="com.deepseek.harness.idea.runtime.DshRuntimeRegistry"/>
        <applicationService serviceImplementation="com.deepseek.harness.idea.runtime.DshLifecycleManager"/>

        <applicationConfigurable parentId="tools"
                                 instance="com.deepseek.harness.idea.settings.DshSettingsConfigurable"
                                 id="dsh.settings"
                                 bundle="messages.DshBundle"
                                 key="settings.displayName"/>
    </extensions>
</idea-plugin>
